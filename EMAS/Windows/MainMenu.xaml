<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EMAS_WPF.Windows"
        xmlns:ViewModel="clr-namespace:WPFViewModel" x:Class="EMAS_WPF.Windows.MainMenu"
        mc:Ignorable="d"
        Title="MainMenu" Height="450" Width="800">
    <Window.Resources>
        <Style TargetType="{x:Type Button}">
            <Setter Property="Margin" Value="3"/>
            <Setter Property="FontSize" Value="20"/>
        </Style>
    </Window.Resources>
    <Window.DataContext>
        <ViewModel:MainMenuVM/>
    </Window.DataContext>
    <Grid>
        <StackPanel>
            <Button Content="Оборудование" Command="{Binding OpenEquipmentWindow}"/>
            <Button Content="О программе" Command="{Binding OpenAboutWindow}"/>
            <Button Content="Окно адмнистрации" Command="{Binding OpenAdminWindow}"/>
        </StackPanel>
    </Grid>
</Window>
